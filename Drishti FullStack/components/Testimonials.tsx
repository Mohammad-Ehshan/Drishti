"use client"

import { motion } from "framer-motion"
import { Card, CardContent } from "@/components/ui/card"
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar"
import { Star } from "lucide-react"

const testimonials = [
  {
    name: "Sarah Johnson",
    role: "Event Safety Director",
    company: "MegaEvents Corp",
    content:
      "Project Drishti transformed our event safety operations. The AI predictions helped us prevent three major incidents last month.",
    avatar: "/placeholder-user.jpg",
    rating: 5,
  },
  {
    name: "Michael Chen",
    role: "Emergency Response Coordinator",
    company: "City Safety Department",
    content:
      "The real-time monitoring and dispatch optimization reduced our response times by 40%. Incredible technology.",
    avatar: "/placeholder-user.jpg",
    rating: 5,
  },
  {
    name: "Emily Rodriguez",
    role: "Festival Operations Manager",
    company: "Summer Music Festival",
    content: "The crowd flow predictions were spot-on. We managed 50,000 attendees without a single safety incident.",
    avatar: "/placeholder-user.jpg",
    rating: 5,
  },
]

export default function Testimonials() {
  return (
    <section className="py-20">
      <div className="container mx-auto px-4">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8 }}
          viewport={{ once: true }}
          className="text-center mb-16"
        >
          <h2 className="text-4xl font-bold text-foreground mb-4">Trusted by Safety Professionals</h2>
          <p className="text-xl text-muted-foreground">See what our users say about Project Drishti</p>
        </motion.div>

        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          {testimonials.map((testimonial, index) => (
            <motion.div
              key={testimonial.name}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.5, delay: index * 0.1 }}
              viewport={{ once: true }}
            >
              <Card className="h-full">
                <CardContent className="p-6">
                  <div className="flex mb-4">
                    {[...Array(testimonial.rating)].map((_, i) => (
                      <Star key={i} className="h-5 w-5 fill-yellow-400 text-yellow-400" />
                    ))}
                  </div>
                  <p className="text-muted-foreground mb-6 italic">"{testimonial.content}"</p>
                  <div className="flex items-center space-x-3">
                    {/* TODO: Add actual testimonial photos here */}
                    <Avatar>
                      <AvatarImage src={testimonial.avatar || "/placeholder.svg"} alt={testimonial.name} />
                      <AvatarFallback>
                        {testimonial.name
                          .split(" ")
                          .map((n) => n[0])
                          .join("")}
                      </AvatarFallback>
                    </Avatar>
                    <div>
                      <p className="font-semibold text-foreground">{testimonial.name}</p>
                      <p className="text-sm text-muted-foreground">
                        {testimonial.role}, {testimonial.company}
                      </p>
                    </div>
                  </div>
                </CardContent>
              </Card>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  )
}
